# Placeholder Nav2 config.
# Replace these values with real robot settings and planner/controller configs.
planner_server:
  ros__parameters:
    expected_planner_frequency: 1.0

controller_server:
  ros__parameters:
    controller_frequency: 10.0
